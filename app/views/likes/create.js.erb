<% if @post.likes.last.likeble_type == "Post" %>
	$(".post-<%=@post.id%>").html("<%= j render partial: 'like', :locals => {:post => @post} %>");
<% else %>
	$(".comment_like-<%=@post.id%>").html("<%= j render partial: 'like_comment', :locals => {:comment => @post} %>");
<% end %>