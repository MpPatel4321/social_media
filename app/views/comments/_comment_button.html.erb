<%= link_to new_comment_path(id: post.id, type: "Post"), remote: true,  class: "navbar-brand comment-#{post.id}" do %>
  <image src="/assets/comment.png" class="avatar float-left">
  <%= post.comments.count %>
<% end %>