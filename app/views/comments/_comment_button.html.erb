<%= link_to new_comment_path(post_id: post.id), remote: true,  class: "navbar-brand comment-#{post.id}" do %>
  <image src="/assets/comment.png" class="avatar float-left">
  <%= post.comments.count %>
<% end %>